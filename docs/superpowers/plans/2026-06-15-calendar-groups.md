# Calendar And Groups Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a public `/calendario` section with schedule and group standings, without bracket/eliminatorias in this phase.

**Architecture:** Reuse existing `matches` and `teams` data. Add one public Convex query that returns all matches and team group metadata needed by the web route, then derive schedule/group view models in focused web helpers.

**Tech Stack:** Convex, TanStack Router, React, Bun tests, Tailwind classes.

---

### Task 1: Backend Calendar Query

**Files:**
- Modify: `packages/backend/convex/matches.ts`
- Test: `packages/backend/convex/publicDashboard.test.ts`

- [ ] Add failing test that public calendar data includes team group codes, match number, venue, status, and scores when live/finished.
- [ ] Implement `getPublicCalendar` query in `matches.ts` using existing indexes and `summarizePublicMatch`-style shape.
- [ ] Verify scheduled scores remain hidden if present accidentally.

### Task 2: Calendar View Models

**Files:**
- Create: `apps/web/src/lib/calendar-groups.ts`
- Create: `apps/web/src/lib/calendar-groups.test.ts`

- [ ] Add failing tests for grouping matches by Guatemala day.
- [ ] Add failing tests for group table calculations: points, goals for/against, goal difference, played, wins/draws/losses.
- [ ] Implement minimal helpers.

### Task 3: Web Route

**Files:**
- Create: `apps/web/src/routes/calendario.tsx`
- Modify: `apps/web/src/routeTree.gen.ts` if generated by route tooling/check-types.
- Modify: `apps/web/src/lib/i18n.tsx`

- [ ] Build route with tabs `Calendario` and `Grupos`.
- [ ] Calendar tab shows date sections and compact match cards.
- [ ] Groups tab shows one group card per group with standings and recent/upcoming fixtures.
- [ ] Keep mobile single-column and desktop multi-column responsive.

### Task 4: Navbar Fit

**Files:**
- Modify: `apps/web/src/lib/navigation.ts`
- Modify: `apps/web/src/components/header.tsx`
- Modify: `apps/web/src/components/user-menu.tsx`

- [ ] Add `Calendario` to primary navigation.
- [ ] Move `Manual` into a compact `Más` menu on desktop/mobile if needed, or keep nav at three primary links plus account/locale.
- [ ] Ensure player/operator header states still fit.

### Task 5: Verification And Deploy

**Files:**
- No code unless generated files change.

- [ ] Run `bun test apps/web/src/lib/calendar-groups.test.ts packages/backend/convex/publicDashboard.test.ts`.
- [ ] Run `bun test`.
- [ ] Run `bun run check-types`.
- [ ] Commit.
- [ ] Deploy Convex backend if query changed.
- [ ] Deploy web.
